# Optional. If set, custom questions work without pasting a key in Settings.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# OPENROUTER_API_KEY=

# Required for live pair pads (shared editor + notes across browsers).
# DATABASE_URL=
